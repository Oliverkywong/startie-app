import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oliverstrat.startie',
  appName: 'startie',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
